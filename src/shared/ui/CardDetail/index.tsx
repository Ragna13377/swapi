import { TNormalizedCharacter } from '@shared/types';

export const CardDetail = ({
	name,
	mass,
	height,
	gender,
	birthYear,
	eyeColor,
	skinColor,
	hairColor,
}: TNormalizedCharacter) => <div>текст</div>;
